#!/bin/bash

# Set the directory where your .desktop files are located
desktop_dir="~/.local/share/applications"

# Define the actions based on the click event
case $1 in
    --left-click)
        # Handle left-click event here
        ;;
    *)
        # Handle default event (when no click event is specified)
        ;;
esac

